'use strict';
function createMaze() {
    alert('pas');
    const canvas = document.getElementById('maze0');
    const context = canvas.getContext('2d');
    context.fillStyle = 'gray';
    context.fillRect(1, 1, 400, 400);
    // context.fillRect(900, 500, 50, 500);
    // context.fillRect(100, 900, 500, 50);
}